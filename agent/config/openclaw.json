{
  "$schema": "https://openclaw.dev/schema/config.json",
  "$comment": "Judgement Agent — GP test (S0) config. Telegram-only, single model.",

  "agents": {
    "defaults": {
      "model": "claude-sonnet-4-20250514",
      "compaction": {
        "model": "claude-sonnet-4-20250514",
        "mode": "default"
      }
    }
  },

  "$comment_channels": "Telegram channel config lives in ~/.openclaw/openclaw.json on the server (has botToken + group IDs). Do NOT duplicate here.",

  "heartbeat": {
    "every": "0m"
  },

  "dmScope": "per-channel-peer",
  "sandbox": "non-main",
  "softThresholdTokens": 20000,
  "reserveTokensFloor": 30000,

  "cron": [
    {
      "id": "daily-digest",
      "schedule": "0 1 * * *",
      "sessionTarget": "isolated",
      "model": "claude-sonnet-4-20250514",
      "task": "Compile daily digest from Supabase scout_submissions: total evaluations today, breakdown by action (REJECT/DB_SAVE/SIGNAL/TRADE), acceptance rate, top scores with reasoning, top scouts by points. Post to team review channel."
    }
  ]
}
